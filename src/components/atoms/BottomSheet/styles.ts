import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  modalContainer: {
    padding: 10,
  },
  headerContainer: {
    paddingBottom: 10,
    alignItems: 'center',
  },
  contentContainer: {
    paddingBottom: 10,
  },
});
