import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
  },
  buttonIcon: {
    width: 90,
    height: 90,
    marginBottom: 5,
    borderRadius: 50,
    resizeMode: 'contain',
    borderColor: '#FC6A04',
    borderWidth: 2,
  },
  buttonText: {
    fontSize: 12,
    color: '#000',
    textAlign: 'center',
    fontFamily: 'Bold',
  },
});