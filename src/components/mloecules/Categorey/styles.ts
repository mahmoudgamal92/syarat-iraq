import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    alignItems: 'center',
    marginHorizontal: 10,
  },
  buttonIcon: {
    width: 100,
    height: 100,
    marginBottom: 5,
  },
  buttonText: {
    fontSize: 14,
    color: '#000',
    textAlign: 'center',
    fontFamily: 'Regular',
  },
});